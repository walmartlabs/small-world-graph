#include "wikipedia_sequential_parser.h"

/*
 * Get a string representation of your tree in JSON
 * The client is responsible for freeing this.
 */
char* wiki_seq_tree_to_json(wiki_seq_tree_t* tree, int beautify = 0);
